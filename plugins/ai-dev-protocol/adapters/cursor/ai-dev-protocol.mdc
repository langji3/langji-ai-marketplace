---
description: AI Dev Protocol for team AI-assisted development workflow
globs:
  - "**/*"
alwaysApply: false
---

# AI Dev Protocol

当 Cursor AI 处理需求实现、代码修改、bug fix、提交或交付时，遵守 AI Dev Protocol。

本协议在 Codex 中拆分为多个 workflow skills；Cursor 使用本 rule 读取同一套核心规则。

分支模式自动判断：AI 在实现前先查看当前分支命名风格。

- 需求分支模式：如果当前分支已经是明确需求分支，例如 `feature/...`、`fix/...`、`hotfix/...`、`task/...` 或 ticket 命名分支，AI 直接在当前分支工作，并跳过 merge-back。
- 个人分支模式：如果当前分支是开发者个人分支，例如 `developer/<name>`、`dev/<name>`、`<name>/dev`，每个明确需求从该分支创建一个 `ai/...` 分支，AI 完成实现和验证后 squash merge 回开发者个人分支。
- 分支含义不明确时，AI 必须先询问当前分支是个人分支还是需求分支。

核心规则：

- 一个 AI 工作单元只处理一个明确 requirement。
- 动手前先确认需求范围；范围不清时必须提问。
- 实现前写中文 spec，包含目标、范围、非目标、影响区域和验证方式。
- 实现前先根据当前分支命名风格选择分支模式。
- 当前分支已是明确需求分支时，直接在当前分支工作并跳过额外 AI 分支。
- 当前分支是个人开发分支时，从该分支创建 `ai/{yyyyMMdd}-{developer}-{short-desc}` 分支。
- 不直接在 `main`、`master`、`dev`、`develop`、`site`、`staging`、`test`、`prod`、`production` 等主干或环境分支上实现。
- spec、交付摘要、Apifox 摘要使用中文；代码标识符、API 路径、表名、配置键保持英文。
- 不混入无关重构、格式化、依赖升级或 workflow 产物。
- commit message 使用中文，需求用 `feat:`，修改用 `fix:`。
- 个人分支模式下，AI 验证后 squash merge 回开发者个人分支；需求分支模式跳过 merge-back。
- 最终交付包含验证结果、merge-back 状态和开发者接管说明；API 变更包含 Apifox sync summary。

阶段对应关系：

- 需求澄清：`ai-requirement-intake`
- 分支规则：`ai-branch-workflow`
- 中文 spec：`ai-spec-writing`
- 范围控制：`ai-implementation-scope`
- 提交规则：`ai-commit-rules`
- Merge-back：`ai-merge-back`
- 最终交付：`ai-handoff`
- API 同步：`ai-apifox-sync`

完整规则以仓库 `skills/` 下各 `SKILL.md` 为准。
